{
  "name": "vixvoxwave-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.4 <21 || >=22.13.0 <23 || >=24.3.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "lint": "eslint .",
    "test": "node --test",
    "check:content": "node scripts/check-content.mjs",
    "check:output": "node scripts/check-built-site.mjs",
    "check": "npm run lint && npm run test && npm run check:content && npm run build && npm run check:output"
  },
  "dependencies": {
    "next": "16.2.12",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.7.0"
  },
  "overrides": {
    "postcss": "8.5.25",
    "sharp": "0.35.3"
  }
}
